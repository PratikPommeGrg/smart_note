import 'package:flutter/material.dart';

class HomeScreenViewModel extends StatelessWidget {
  const HomeScreenViewModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
